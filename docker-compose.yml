services:
  mysql:
    image: mysql:8.0
    container_name: recarga-facil-mysql
    environment:
      MYSQL_DATABASE: recargafacil
      MYSQL_ROOT_PASSWORD: recarga123
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: